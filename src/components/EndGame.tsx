import * as React from 'react';
import styles from './EndGame.module.css';

export interface IEndGameProps {
}

export default function EndGame (props: IEndGameProps) {
  return (
    <div>
      
    </div>
  );
}
